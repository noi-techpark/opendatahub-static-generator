services:

  generator-worker:
    image: ${DOCKER_IMAGE}:${DOCKER_TAG}
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - static-data:/data
    environment:
      Sync__OutputPath: /data
    restart: unless-stopped

volumes:
  static-data:
